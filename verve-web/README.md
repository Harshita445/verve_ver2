# verve-web

Next.js 15 + TypeScript + Tailwind frontend for Verve.

## Setup

```bash
cp .env.example .env.local
npm install
npm run dev
```

## Testing

```bash
npx vitest run          # unit / component tests
npx playwright test     # E2E tests
```

## Build

```bash
npm run build
```
